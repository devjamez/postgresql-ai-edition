# pg_ai — PostgreSQL AI Edition (thin edition): embeddings, semantic search, RAG, agents
comment = 'PostgreSQL AI Edition: embeddings, semantic search, RAG and agents inside PostgreSQL'
default_version = '0.9.0'
requires = 'vector, plpython3u'
relocatable = false
