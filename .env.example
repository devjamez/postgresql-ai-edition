# Local inference via Ollama is the default — NO API KEY NEEDED.
# This file is only for the OPTIONAL Claude provider (ai.complete_claude).
# Leave empty unless you want answers generated by Anthropic's API.
ANTHROPIC_API_KEY=
