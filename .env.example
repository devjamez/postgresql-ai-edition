# Copy this file to `.env` and fill in your key(s). `.env` is gitignored.
# OpenAI is REQUIRED (embeddings + completion).
OPENAI_API_KEY=sk-...

# Optional: enables ai.complete_claude() for answers generated by Claude.
ANTHROPIC_API_KEY=
