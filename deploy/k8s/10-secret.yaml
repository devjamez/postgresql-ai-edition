apiVersion: v1
kind: Secret
metadata:
  name: pgai-secrets
  namespace: pg-ai
type: Opaque
stringData:
  POSTGRES_PASSWORD: "change-me"
  # Optional: enable the Claude provider (ai.complete_claude)
  # ANTHROPIC_API_KEY: ""
